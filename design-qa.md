# Design QA — 梅花直播中控画面工作台 V5

## 本轮目标

- 采用用户选定的第 3 版设计方向，将工作台重构为“画布优先”布局。
- 消除原有大块无效留白、上下堆叠卡片和长期占位的素材/来源区域。
- 明确产品边界：中控画布只做 OBS 排版参考，最终位置、裁剪和层级仍由 OBS 管理。

## 视觉证据

- 选定设计：`artifacts/ui-v5-qa/selected-option-3.png`
- 最终实现：`artifacts/ui-v5-qa/implementation-v5.png`
- 同画布对照：`artifacts/ui-v5-qa/comparison-v5.png`

## 已完成的产品级整改

- P1 已修复：中心竖屏预览占比过小。工作台现在由 9:16 主舞台占据视觉中心，左侧图层与右侧属性面板均可收起。
- P1 已修复：模块设置和预览相互割裂。选中任意来源后，画布选框、右侧属性和浮动干净预览同步切换。
- P1 已修复：弹窗不可拖拽。当前模块干净预览已实现拖拽定位、关闭、重新显示和独立窗口打开。
- P1 已修复：画布参考无法调整。选中模块可在 1080×1920 参考舞台中拖动和缩放，并支持 10 像素吸附。
- P2 已修复：OBS 来源列表长期占据页面。12 个独立来源改为按需打开的来源目录，仍保留固定地址、复制和预览入口。
- P2 已修复：人物和背景设置挤占工作台。素材上传与阶段映射改为独立弹窗。
- P2 已修复：设置层级混乱。右侧属性拆分为“内容 / 外观 / 动效”三个任务型页签。
- P2 已修复：全局侧栏挤压画布。进入工作台后自动切为图标窄栏，离开工作台不影响其他页面。
- P2 已修复：场景状态切换分散。IDLE、CASTING、SPEAKING、GIFT、QUEUE、ERROR 统一固定在底部场景栏。

## 功能验收

- 图层选择：选择“卦象”后，属性面板标题与预览内容同步更新。
- 属性页签：“外观”页签可真实切换，并显示尺寸、字体、颜色、亮度和主题预设。
- 面板收起：左侧图层收起后中心画布宽度从 722px 扩展至 946px，恢复按钮可正常返回。
- 来源目录：弹窗真实展示 12 个 OBS 独立地址、启用状态、复制与打开入口。
- 拖拽移动：卦象模块在参考舞台上的坐标发生真实变化。
- 拖拽缩放：卦象模块从 374.4×201.6px 缩放为 381.6×230.4px，交互生效。
- 浮动预览：可关闭、重新显示、拖拽，并与当前选中来源使用同一预览会话。
- 正式边界：页面明确提示“参考排版，不写入 OBS 位置”，没有伪装成 OBS 场景控制。

## 工程验收

- `pnpm --filter @meihua/admin typecheck`：通过。
- `pnpm --filter @meihua/admin build`：通过。
- 生产构建已更新，当前 `http://127.0.0.1:5200/` 已加载新版工作台。
- 本轮未修改后端协议、数据库结构或正式 OBS 地址。

## 结果

选定的第 3 版画布优先方向已完整落地；当前范围内没有未解决的 P1/P2 视觉或核心交互问题。

final result: passed
